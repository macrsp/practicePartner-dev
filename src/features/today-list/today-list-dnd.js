/**
 * @role interaction-helper
 * @owns Today-list drag-and-drop wiring
 * @not-owns Today-list persistence, validation, or presentation markup
 * @notes This is a placeholder adapter until the Today-list UI surface is present.
 */

export function createTodayListDnd() {
  return {
    destroy() {},
  };
}